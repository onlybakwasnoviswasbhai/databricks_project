layer: marts
name: dim_customers
description: Customer dimension kept in sync with the source system.
refresh:
  mode: full_compare
columns:
  - name: customer_id
    data_type: bigint
    nullable: false
    primary_key: true
  - name: email
    data_type: string
  - name: country
    data_type: string
  - name: signed_up_at
    data_type: timestamp
